apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: aws-secrets
  namespace: ns-payments
spec:
  provider: aws
  secretObjects:
    - secretName: database-url
      type: Opaque
      data:
        - objectName: "db-payments-url-secret"
          key: url
  parameters:
    objects: |
      - objectName: "db-payments-url-secret"
        objectType: "secretsmanager"
  